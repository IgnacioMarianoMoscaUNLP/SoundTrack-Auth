    # application.properties
    spring.data.mongodb.host=localhost
    spring.data.mongodb.port=27017
    spring.data.mongodb.database=soundtrack_db
    # Optional: If authentication is required
    spring.data.mongodb.username=admin
    spring.data.mongodb.password=pass1
    # Optional: For MongoDB Atlas or complex connections
    ### spring.data.mongodb.uri=mongodb+srv://<username>:<password>@cluster0.mongodb.net/<databaseName>?retryWrites=true&w=majority
 #     security.jwt.secret-key=unaClaveSecreta123
#security.jwt.expiration-time=3600000
#security.jwt.algorithm=HS256 
# Puerto HTTPS
server.port=8443

# Configuración SSL
server.ssl.key-store=classpath:keystore.p12
server.ssl.key-store-password=your_keystore_password
server.ssl.key-store-type=PKCS12
server.ssl.key-alias=localhost

# Opcional: deshabilitar HTTP
server.ssl.enabled=true
